##  Quy ước đặt tên của flyway: V{số version}__{mô tả}.sql 
(VD: V1__create_tables.sql)

## Chạy môi trường dev

1. Khởi động hạ tầng local:
`docker compose up -d`

2. Tạo file `.env` từ `.env.example`

3. Chạy ứng dụng Spring Boot
